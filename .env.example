# ============================================================
# POSTGRES (used by docker-compose to init the postgres container)
# DATABASE_URL in apps/api/.env must match these values
# ============================================================
POSTGRES_USER=cvoptimizer
POSTGRES_PASSWORD=changeme
POSTGRES_DB=cv_optimizer

# ============================================================
# PORTS (optional — defaults shown)
# ============================================================
POSTGRES_PORT=5432
PORT=3001
WEB_PORT=3000
