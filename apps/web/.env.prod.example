# ============================================================
# WEB PRODUCTION ENV — copy to apps/web/.env.prod
# ============================================================

# Must be the public domain — no trailing slash
NEXT_PUBLIC_API_URL=https://YOUR_DOMAIN
