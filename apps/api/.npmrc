# Flat node_modules (npm-like) for the standalone Docker build.
# Without this, pnpm's strict symlink layout breaks:
#   - tsc TS2742: transitive @types (express-serve-static-core, qs) aren't nameable
#   - phantom dep: googleapis' `google-auth-library` import fails to resolve
shamefully-hoist=true
